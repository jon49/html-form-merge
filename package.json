{
  "name": "html-form-merge",
  "type": "module",
  "version": "0.0.0",
  "description": "Merge DOM elements for html-form",
  "main": "html-form-events.js",
  "homepage": "https://github.com/jon49/html-form-merge#readme",
    "bugs": {
        "url": "https://github.com/jon49/html-form-merge/issues"
    },
  "scripts": {
    "start": "esbuild html-form-merge.js --format=iife --bundle --outdir=public && esbuild ./public/html-form-merge.js --minify --outfile=public/html-form-merge.min.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "htmf",
    "html-form",
    "html",
    "merge"
  ],
  "author": "Jon Nyman",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.25.10",
    "morphdom": "^2.7.7"
  }
}
